<?php 
			/*$tummesaj  = imap_open('{imap.gmail.com:993/imap/ssl/novalidate-cert}INBOX', "fff@f.com", "f");
			$basliklar = imap_header($tummesaj);

			function donustur($string){
				$string =str_replace("=?iso-8859-1?q?","",$string);
				$string =str_replace("=?iso-8859-1?Q?","",$string);
				$string =str_replace("=?ISO-8859-1?Q?","",$string);
				$string =str_replace("=?","",$string);

			
				$charHex = array("0","1","3","4","5","6","7","8","9","A","B","C","D","E","F");
				for($z=0;$z<sizeof($charHex);$z++){
					for($i=0;$i<sizeof($charHex);$i++){
						$string = str_replace(("=".($charHex[$z].$charHex[$i])),chr(hexdec($charHex[$z].$charHex[$i])),$string);

					}
				}
				return($string);
			}
			echo count($basliklar);*/


		?>